[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "oscilador"
version = "0.1.0"
requires-python = ">=3.7"
# NO agregues pyo3 aquí, ya que se compila dentro del binario